package chat.jaspr.apocalypse;

/**
 * Removed. StatLifeStore held the stat persistence layer, which has been rebuilt from scratch
 * in the JasprRPG plugin (package {@code chat.jaspr.rpg}) against the original mod's own numbers.
 *
 * Nothing references this class any more. It remains only as an empty marker so that an older
 * checkout of this folder still compiles; it can be deleted outright at any time.
 */
final class StatLifeStore {
    private StatLifeStore() {}
}
